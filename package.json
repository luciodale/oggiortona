{
  "name": "oggi-a-ortona",
  "type": "module",
  "version": "0.1.0",
  "description": "Mangiare e fare a Ortona",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "bun run build && wrangler dev",
    "deploy": "bun run build && wrangler deploy",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 execute oggi-a-ortona-db --file=src/db/migrations/0001_initial.sql --local",
    "db:seed:local": "wrangler d1 execute oggi-a-ortona-db --file=src/db/seed.sql --local",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "astro check && tsc --noEmit",
    "lint": "echo 'No linter configured yet'"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/cloudflare": "12",
    "@astrojs/react": "^4.3.0",
    "@astrojs/sitemap": "^3.5.0",
    "@tailwindcss/vite": "^4.1.12",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "astro": "^5.13.2",
    "leaflet": "^1.9.4",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "tailwindcss": "^4.1.12",
    "typescript": "^5.8.3",
    "wrangler": "^4.76.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "@types/leaflet": "^1.9.21",
    "vitest": "^3.2.1"
  }
}
